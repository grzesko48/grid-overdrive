# Plik wynikowy do publikacji artefaktu — powstaje przy każdym build_merged.py.
# Nie trzymamy go w repozytorium, bo to 11 MB duplikatu treści index.html.
artifact.html

# Katalog roboczy porównań katalogów sklepowych (dane tymczasowe)
recheck/

# Surowy zrzut z pobierania (173 KB na przebieg — 60 MB rocznie, gdyby go trzymać).
# Wnioski z niego trafiają do raport_rano.json i coverage_log.json, i te commitujemy.
zebrane_ceny.json
